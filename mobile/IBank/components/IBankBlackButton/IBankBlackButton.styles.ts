import styled from 'styled-components/native';
import { colors } from '../../config/colors';

export const BlackButtonTouchable = styled.TouchableOpacity`
  background-color: ${colors.gray900};
  padding: 12px 0;
  border-radius: 10px;
`;
